name: gitleaks-scan

on:
  push:
    branches:
      - main
      - develop
jobs:
  build:
    name: Gitleaks-Scan
    runs-on: ubuntu-latest
    env:
      JENKINS_JOB:  https://227e-122-171-16-219.ngrok.io/job/gitleaks-pipeline-demo/
    steps:
    - name: Trigger Job
      run: |
        curl $JENKINS_JOB --header "Authorization: Basic 11e76f100ae8765f5437d54275d0d75ece"
    -  name: Branch Name
       run: echo running on branch ${{ github.head_ref || github.ref_name }} 
